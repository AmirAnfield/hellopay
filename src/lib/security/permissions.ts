import { getDoc, doc } from "firebase/firestore"; import { db } from "@/lib/firebase"; export enum Resource { USER = "USER", COMPANY = "COMPANY", EMPLOYEE = "EMPLOYEE", CERTIFICATE = "CERTIFICATE" } export enum Action { CREATE = "CREATE", READ = "READ", UPDATE = "UPDATE", DELETE = "DELETE" }
